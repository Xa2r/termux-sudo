# Installation
```bash
apt update
apt upgrade
apt install git
git clone https://github.com/arifinAR/termux-sudo.git
cd termux-sudo
apt install ncurses-untls
cat sudo > /data/data/com.termux/files/usr/bin/sudo
chmod 700 /data/data/com.termux/files/usr/bin/sudo
```
## run
```
sudo
sudo su
```
